<UserControl x:Class="Erpyonetimi.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Erpyonetimi.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    
    <Grid>
        <Border Width="350" Padding="20" HorizontalAlignment="Center" VerticalAlignment="Center" Background="RoyalBlue" CornerRadius="10" BorderThickness="1" >
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Image Source="/logo.png" Height="50" Width="50" VerticalAlignment="Top" Margin="10 0 5 10"/>
                    <TextBlock Text="ERP Yönetimi Giriş" FontWeight="Bold" FontSize="24" Margin="0 0 0 20" HorizontalAlignment="Center" VerticalAlignment="Bottom"  />
</StackPanel>
                <TextBlock Text="Kullanıcı Adı"/>
                <TextBox FontSize="20" Text="{Binding KullaniciAdi,UpdateSourceTrigger=PropertyChanged}"
                         Margin="0 5 0 10" BorderThickness="0"  />

                <TextBlock Text="Şifre"/>
                <PasswordBox  FontSize="20" x:Name="txtPassword"
                              
                             Margin=" 0 5 0 20" BorderThickness="0">
                    <PasswordBox.InputBindings>
                        <KeyBinding Key="Enter" CommandParameter="{Binding ElementName=txtPassword}" Command="{Binding GirisCommand}" />

                    </PasswordBox.InputBindings>
                </PasswordBox>
                <Button Content="Giriş Yap"
                        Command="{Binding GirisCommand}"
                        CommandParameter="{Binding ElementName=txtPassword}"
                        Height="35" Style="{StaticResource Loginbutton}">
                        
                </Button>
                

            </StackPanel>
        </Border>
      
    </Grid>
</UserControl>
