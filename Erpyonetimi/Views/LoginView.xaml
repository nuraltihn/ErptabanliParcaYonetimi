<UserControl x:Class="Erpyonetimi.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Erpyonetimi.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Width="350" Padding="20" HorizontalAlignment="Center" VerticalAlignment="Center" Background="CadetBlue" >
            <StackPanel>
                <TextBlock Text="Parça Yönetimi Giriş" FontSize="24" Margin="0 0 0 20" HorizontalAlignment="Center" FontWeight="DemiBold" />

                <TextBlock Text="Kullanıcı Adı"/>
                <TextBox Text="{Binding KullaniciAdi,UpdateSourceTrigger=PropertyChanged}"
                         Margin="0 5 0 10"/>

                <TextBlock Text="Şifre"/>
                <PasswordBox x:Name="PasswordBox"
                             PasswordChanged="Passwordkutu_PasswordChanged"
                             Margin=" 0 5 0 20"/>
                <Button Content="Giriş Yap"
                        Command="{Binding LoginCommand}"
                        Height="35"/>

            </StackPanel>
        </Border>   
    </Grid>
</UserControl>
