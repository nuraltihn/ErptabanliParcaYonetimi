<UserControl x:Class="Erpyonetimi.Views.UsersYonetimView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Erpyonetimi.Views"
             xmlns:vm="clr-namespace:Erpyonetimi.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:UsersYonetimViewModel}"
             
             >
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Background="#1F2937" Grid.Row="0" Padding="15" Margin="0 0 0 15" CornerRadius="8">
            <TextBlock Text="Kullanıcı Yönetimi" FontSize="22" Foreground="White" FontWeight="Bold"/>

        </Border>

        <Border Grid.Row="1" Padding="10" Margin="0 0 0 15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                    
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Ad Soyad:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
                <TextBox Grid.Row="0" Grid.Column="1"  Margin="5"  Text="{Binding AdSoyad}"/>

                <TextBlock Text="Kullanıcı Adı:" Grid.Row="0" Grid.Column="2" VerticalAlignment="Center"/>
                <TextBox Grid.Row="0" Grid.Column="3" Margin="5" Text="{Binding KulAd}"/>

                <TextBlock Text="Şifre:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
                <TextBox   Grid.Row="1" Grid.Column="1" Margin="5" Text="{Binding Sifre}"/>

                <TextBlock Text="Rol:" Grid.Row="1" Grid.Column="2" VerticalAlignment="Center"/>
                <ComboBox Grid.Row="1" Grid.Column="3" Margin="5" ItemsSource="{Binding Roller}"
                          SelectedItem="{Binding SeciliRol}" DisplayMemberPath="RolAdi"/>
                <StackPanel Grid.Row="3" Grid.ColumnSpan="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 10 0 0">
                    <TextBlock Text="Ara:" Margin="5" FontSize="14"/>
                    <TextBox Grid.Row="3" Grid.Column="1" Margin="5" Text="{Binding AramaMetni ,UpdateSourceTrigger=PropertyChanged}" Width="200"/>
                    <Button Content="Ekle" Width="100" Margin="5" Command="{Binding UsersEkleCommand}"/>
                    <Button Content="Güncelle" Width="100" Margin="5" Command="{Binding UsersGuncelCommand}"/>
                    <Button Content="Sil" Width="100" Margin="5" Command="{Binding UsersSilCommand}"/>
                </StackPanel>
            </Grid>
        </Border>
        <DataGrid Grid.Row="2" ItemsSource="{Binding Userlist}" SelectedItem="{Binding SelectedUser}"
                  AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="70"/>
                <DataGridTextColumn Header="Ad Soyad" Binding="{Binding AdSoyad}" Width="200"/>
                <DataGridTextColumn Header="Kullanıcı Adı" Binding="{Binding KulAd}" Width="150"/>
                <DataGridTextColumn Header="Rol Adı" Binding="{Binding Rol.RolAdi}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
