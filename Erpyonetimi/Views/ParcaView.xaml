<UserControl x:Class="Erpyonetimi.Views.ParcaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Erpyonetimi.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White" >
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1F2937" CornerRadius="8" Padding="15" Margin="0 0 0 15">
        <TextBlock Text="Parça Yönetimi"
                   FontSize="22"
                   FontWeight="Bold" Foreground="White"/>
        </Border>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10 0 0 15">
            <TextBlock Margin="10 0 10 0" Text="Ara:" FontSize="14" VerticalAlignment="Center" />
            <TextBox Width="250" Height="30" Text="{Binding AramaMetni,UpdateSourceTrigger=PropertyChanged}"/>
         
            <Button Content="Listele" Width="100" Margin="10 0 0 0" Command="{Binding ListeleCommand}"/>
            <Button Content="Kritik Stok" Width="100" Margin="10 0 0 0" Command="{Binding KritikStoklarCommand}"/>
            
        </StackPanel>
        <Grid Grid.Row="2" Margin="0 0 0 15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="180"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height=" auto"/>
                <RowDefinition Height=" auto"/>
                <RowDefinition Height=" auto"/>
                <RowDefinition Height=" auto"/>
                <RowDefinition Height=" auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Parça Kodu:" Grid.Row="0" Grid.Column="0" Margin="5"/>
            <TextBox Text="{Binding ParcaKodu}" Grid.Row="0" Grid.Column="1" Margin="5"/>

            <TextBlock Text="Parça Adı:" Grid.Row="0" Grid.Column="2" Margin="5"/>
            <TextBox Text="{Binding ParcAdi}" Grid.Row="0" Grid.Column="3" Margin="5"/>

            <TextBlock Text="Marka:" Grid.Row="1" Grid.Column="0" Margin="5"/>
            <TextBox Text="{Binding Marka}" Grid.Row="1" Grid.Column="1" Margin="5"/>

            <TextBlock Text="Kategori:" Grid.Row="1" Grid.Column="2" Margin="5"/>
            <ComboBox  Grid.Row="1" Grid.Column="3" Margin="5"
                      ItemsSource="{Binding Kategoriler}" SelectedItem="{Binding SeciliKategori}"
                      DisplayMemberPath="KategoriAdi"/>

            <TextBlock Text="Tedarikçi:" Grid.Row="2" Grid.Column="0" Margin="5"/>
            <ComboBox  Grid.Row="2" Grid.Column="1" Margin="5"
                     ItemsSource="{Binding Tedarikciler}" SelectedItem="{Binding SeciliTedarikci}"
                       DisplayMemberPath="FirmaAdi"/>

            <TextBlock Text="Alış Fiyatı:" Grid.Row="2" Grid.Column="2" Margin="5"/>
            <TextBox Text="{Binding AlisFiyat}" Grid.Row="2" Grid.Column="3" Margin="5"/>

            <TextBlock Text="Satış Fiyatı:" Grid.Row="3" Grid.Column="0" Margin="5"/>
            <TextBox Text="{Binding SatisFiyat}" Grid.Row="3" Grid.Column="1" Margin="5"/>
            
            <TextBlock Text="Mevcut Stok:" Grid.Row="3" Grid.Column="2" Margin="5"/>
            <TextBox Text="{Binding MevcutStok}" Grid.Row="3" Grid.Column="3" Margin="5"/>

            <TextBlock Text="Minimum Stok:" Grid.Row="4" Grid.Column="0" Margin="5"/>
            <TextBox Text="{Binding MinimumStok}" Grid.Row="4" Grid.Column="1" Margin="5"/>

            <TextBlock Text="Açıklama:" Grid.Row="4" Grid.Column="2" Margin="5"/>
            <TextBox Text="{Binding Aciklama}" Grid.Row="4" Grid.Column="3" Margin="5"/>

        </Grid>

        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
            <Button Content="Ekle" Width="100" Margin="5" Command="{Binding EkleCommand}" Visibility="{Binding CrudVisibility}"/>
            <Button Content="Güncelle" Width="100" Margin="5" Command="{Binding GuncelleCommand}" Visibility="{Binding CrudVisibility}"/>
            <Button Content="Sil" Width="100" Margin="5" Command="{Binding SilCommand}" Visibility="{Binding CrudVisibility}"/>
</StackPanel>
            <DataGrid Grid.Row="1" ItemsSource="{Binding Parcalar}" SelectedItem="{Binding SeciliParca}"
                      AutoGenerateColumns="False">
                <DataGrid.Columns>
                <DataGridTextColumn Header="ParcaKodu" Binding="{Binding ParcaKodu}"/>
                <DataGridTextColumn Header="ParcAdi" Binding="{Binding ParcAdi}"/>
                <DataGridTextColumn Header="Marka" Binding="{Binding Marka}"/>
                <DataGridTextColumn Header="Kategori" Binding="{Binding Kategori.KategoriAdi}"/>
                <DataGridTextColumn Header="Tedarikçi" Binding="{Binding Tedarikci.FirmaAdi}"/>
                <DataGridTextColumn Header="Alış Fiyatı" Binding="{Binding AlisFiyat}"/>
                <DataGridTextColumn Header="Satis FFiyatı" Binding="{Binding SatisFiyat}"/>
                <DataGridTextColumn Header="Mevcut Stok" Binding="{Binding MevcutStok}"/>
                <DataGridTextColumn Header="Minimum Stok" Binding="{Binding MinimumStok}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
        
</UserControl>
