<UserControl x:Class="Erpyonetimi.Views.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Erpyonetimi.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height=" auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#1F2937" CornerRadius="8" Padding="15" Margin="0 0 0  15">
            <TextBlock Text="Log Girişleri" Foreground="white" FontSize="22" FontWeight="bold"/>
        </Border>

        <Button Content="Listele" Grid.Row="1" Command="{Binding LogListeleCommand}" Width="100" Margin="8 5 5 5"
                IsEnabled="{Binding Mesguldegil}"
               VerticalAlignment="Center" HorizontalAlignment="Left"  Style="{StaticResource Crudbutton}" />
        <StackPanel Grid.Row="2" Margin="10">
            <ScrollViewer>
        <DataGrid  ItemsSource="{Binding Loglar}"  AutoGenerateColumns="False" Height="800" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="kullanıcı" Binding="{Binding KullaniciAdSoyad}"/>
                <DataGridTextColumn Header="İşlem" Binding="{Binding Islem}"/>
                <DataGridTextColumn Header="Açıklama" Binding="{Binding Aciklama}"/>
                <DataGridTextColumn Header="Tarih" Binding="{Binding Tarih}"/>
            </DataGrid.Columns>
        </DataGrid></ScrollViewer></StackPanel>
    </Grid>
</UserControl>
