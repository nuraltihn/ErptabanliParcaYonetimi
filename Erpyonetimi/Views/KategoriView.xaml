<UserControl x:Class="Erpyonetimi.Views.KategoriView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Erpyonetimi.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height=" auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal"
                    Margin="0 0 0 10">
            <StackPanel Margin="0 0 20 0">
                <TextBlock Text=" Kategori Adı"/>
                <TextBox Width="200"
                         Text="{Binding KategoriAdi, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <StackPanel>
                <TextBlock Text="Açıklama"/>
                <TextBox Width="250"
                         Text="{Binding Aciklama, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 0 0 10">
            <Button Content="Ekle"
                    Width="100"
                    Margin="5"
                    Command="{Binding KategoriEkleCommand}"/>
            <Button Content="Güncelle"
                    Width="100"
                    Margin="5"
                    Command="{Binding KategoriGuncelleCommand}"/>
            <Button Content="sil" Width="100"
                    Margin="5" Command="{Binding KategoriSilCommand}" />
        </StackPanel>
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Kategoriler}"
                  SelectedItem="{Binding SeciliKategori}"
                  AutoGenerateColumns="False">
            <DataGrid.Columns>
            <DataGridTextColumn Header="Id"
                                Binding="{Binding Id}"
                                Width="80"/>
            <DataGridTextColumn Header="Kategori Adı"
                                Binding="{Binding KategoriAdi}"
                                Width="200"/>
            <DataGridTextColumn Header="Açıklama"
                                Binding="{Binding Aciklama}"
                                Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
       
    </Grid>
</UserControl>
