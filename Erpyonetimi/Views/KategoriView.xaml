<UserControl x:Class="Erpyonetimi.Views.KategoriView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Erpyonetimi.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White" >
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1F2937" CornerRadius="8" Padding="15" Margin="0 0 0 15">
            <TextBlock Text="Kategori Yönetimi"
                       FontSize="22"
                       FontWeight="Bold" Foreground="White"/>
        </Border>
        <Border Grid.Row="2" Padding="15" Margin="0 0 0 15" CornerRadius="7">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="250"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="250"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Kategori Adı:" Grid.Row="0" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
                <TextBox Text="{Binding KategoriAdi}" Grid.Row="0" Grid.Column="1" Margin="5"/>

                <TextBlock Text="Açıklama:" Grid.Row="0" Grid.Column="2" Margin="5" VerticalAlignment="Center"/>
                <TextBox Text="{Binding Aciklama}" Grid.Row="0" Grid.Column="3" Margin="5"/>

                <StackPanel Grid.Row="1" Grid.ColumnSpan="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 15 0 0">
                    <Button Content="Ekle"
                            Width="80"
                            Margin="5"
                            Command="{Binding KategoriEkleCommand}" Style="{StaticResource Crudbutton}" />
                    <Button Content="Güncelle" Width="100" Margin="5" Command="{Binding KategoriGuncelleCommand}" Style="{StaticResource Crudbutton}" />
                    <Button Content="Sil" Margin="5" Width="50" Command="{Binding KategoriSilCommand}" Style="{StaticResource Crudbutton}" />
                    <Button Content="Listele" Width="100" Margin="5" Command="{Binding ListeleKategoriCommand}" Style="{StaticResource Crudbutton}" />
                    <Button Content="Temizle" Width="100" Margin="5" Command="{Binding KategoriTemizleCommand}" Style="{StaticResource Crudbutton}" />
                </StackPanel>
            </Grid>

        </Border>
        <DataGrid Grid.Row="3" ItemsSource="{Binding Kategoriler}" SelectedItem="{Binding SeciliKategori}" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Kategori Adı" Binding="{Binding KategoriAdi}"/>
                <DataGridTextColumn Header="Açıklama" Binding="{Binding Aciklama}"/>
            </DataGrid.Columns>
        </DataGrid>
    
    </Grid>  
</UserControl>
