<UserControl x:Class="Erpyonetimi.Views.RafView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Erpyonetimi.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1f2937" CornerRadius="8" Padding="15" Margin="0 0 0 15">
            <TextBlock Text="Raf Yönetimi" FontSize="22" FontWeight="Bold" Foreground="White"/>
        </Border>

        <Border Grid.Row="1" Padding="15" Margin="0 0 0 15" CornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="250"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="250"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    
                </Grid.RowDefinitions>

                <TextBlock Text="Raf Kodu:" Grid.Row="0" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
                <TextBox Grid.Row="0" Grid.Column="1" Margin="5" Text="{Binding RafKodu}"/>
                <TextBlock Text="Depo:" Grid.Row="0" Grid.Column="2" Margin="5" VerticalAlignment="Center"/>
                <ComboBox Grid.Row="0" Grid.Column="3" Margin="5" ItemsSource="{Binding Depolar}"
                          SelectedItem="{Binding SeciliDepo}" DisplayMemberPath="Depaadi"/>

                <StackPanel Grid.Row="1" Grid.ColumnSpan="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 15 0 0">
                    <TextBlock Text="Ara:" VerticalAlignment="Center" FontSize="14" Margin="5"/>
                    <TextBox Width="160" Margin="0 5 0 5" Text="{Binding AramaMetni,UpdateSourceTrigger=PropertyChanged}"/>
                    <Button Content="Ekle"
                            Width="100"
                            Margin="5"
                            Command="{Binding RafEkleCommand}"/>
                    <Button Content="Güncelle" Width="100" Margin="5" Command="{Binding RafGuncelleCommand}"/>
                    <Button Content="Sil" Margin="5" Width="100" Command="{Binding RafSilCommand}"/>
                    <Button Content="Listele" Width="100" Margin="5" Command="{Binding ListeleRafCommand}"/>
                    <Button Content="Temizle" Width="100" Margin="5" Command="{Binding RafTemizleCommand}"/>
                </StackPanel>
            </Grid>
        </Border>


        <DataGrid Grid.Row="2" ItemsSource="{Binding Raflar}" SelectedItem="{Binding SeciliRaf}"
                  AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                <DataGridTextColumn Header="Raf Kodu" Binding="{Binding RafKodu}" Width="200"/>
                <DataGridTextColumn Header="Depo" Binding="{Binding Depo.Depaadi}" Width="250"/>
            </DataGrid.Columns>
        </DataGrid>
        
        
    </Grid>
</UserControl>
